# Data Configs

Data configs describe processed datasets and task-level metadata. They should
not contain model hyperparameters.

Use this area for reusable paths and task descriptors shared by preprocessing,
training, inference, and evaluation.

