# Experiment Configs

Experiment configs should compose data, model, training, inference, and
evaluation settings for a run.

The initial files here are architecture placeholders. They intentionally do not
define a final model because the network design is still being discussed.

