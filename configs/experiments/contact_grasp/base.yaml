experiment_name: contact_grasp_base
stage: contact_grasp_generation

data:
  dataset_config: configs/data/datasets/hand_pouring_lfv.yaml
  task_config: configs/data/tasks/hand_pouring.yaml
  input_keys:
    - points_object_m
    - points_object_norm
    - normals_camera
    - dinov2_point_features
  target_keys:
    - contact_heat
    - grasp_pseudo_label

model:
  package: lfv.models.contact_grasp_generation
  point_encoder: todo
  contact_generator: conditional_diffusion_todo
  grasp_head: todo

training:
  trainer_package: lfv.training.contact_grasp
  batch_size: null
  num_epochs: null
  learning_rate: null

inference:
  package: lfv.inference.contact_grasp
  num_contact_samples: null
  num_grasp_samples_per_contact: null

evaluation:
  package: lfv.evaluation.contact_grasp
  metrics:
    - contact_heat_overlap
    - grasp_contact_consistency
    - tcp_depth_diagnostics

