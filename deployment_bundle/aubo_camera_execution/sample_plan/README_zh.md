# 样例计划目录

本机交付目录中可以放置一次已经生成的 `camera_plan.npz`、复核 PNG/PLY 和 JSON 报告，方便发送前检查。出于隐私和可复现性考虑，GitHub 只保留本说明；真实样例文件不会进入公共仓库。

规划机生成新的计划后，将输出目录中的 `camera_plan.npz` 复制到本目录（或直接把输出目录发送到机器人电脑），并按上级 `README_zh.md` 的 dry-run 流程检查。

