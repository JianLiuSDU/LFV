# Evaluate Scripts

Future wrappers for offline metric computation and diagnostics.

Immediate priority:

- grasp TCP depth diagnostics;
- approach candidate comparison;
- contact heat and grasp consistency checks.

