# Inference Scripts

Future command-line wrappers for inference:

- sample contact heat fields and grasp candidates;
- sample functional motion trajectories;
- run the two-stage pipeline.

Keep implementation in `lfv/inference/`.

