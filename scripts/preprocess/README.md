# Preprocess Scripts

Stable preprocessing entrypoints should live here after migration.

Current working entrypoints remain:

- `scripts/run_pipeline.py`
- `scripts/run_hand_pouring_contact_batch.sh`
- `scripts/run_hand_pouring_grasp_batch.sh`

Do not remove them until the new wrappers are validated.

