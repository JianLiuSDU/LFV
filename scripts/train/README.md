# Train Scripts

Future command-line wrappers for training:

- contact-grasp generation;
- functional motion generation;
- optional joint or staged training.

Keep implementation in `lfv/training/`; scripts should only parse configs and
launch jobs.

